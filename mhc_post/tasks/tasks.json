{
  "_comment": "参赛题目清单。run_all.py 读取本文件决定跑哪些 task。",
  "_baseline_note": "aten_ops = v0 每次 forward 的有效 aten 算子调用数（已扣除 view/reshape 等无 kernel 的），由 bench/count_ops.py --update 在沐曦 C500 上实测填入。",
  "tasks": [
    {
      "name": "mhc_post",
      "target_speedup": "14-15x（实测）",
      "kind": "launch-bound",
      "aten_ops": 136,
      "note": "136 个 aten 算子融成 1 个 Triton kernel；Sinkhorn 迭代改写成对角缩放形式，绕开沐曦编译器在 2D loop-carried tile 上的段错误"
    }
  ]
}